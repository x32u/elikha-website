/**
 * Colors available in the AR activity picker.
 *
 * Re-exported from `src/utils/arColorPalette.js` so the AR components and the
 * grading pipeline share one list. Edit the palette there, not here.
 */
export { AR_COLOR_PALETTE as AR_PRESET_COLORS } from '../../../utils/arColorPalette';
